# CLAUDE.md

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues (`FlorinPopaCodes/termoficare-data`) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
